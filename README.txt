## Installing and running the App
1. Clone the repo
2. At the root of the /server folder run "npm install"
3. At the root of the /client folder run "npm install"
4. At the root of the /server folder run "npm run build"
5. At the root of the /server folder run "npm run dev"
6. Visit "localhost:3000" in your browser.
